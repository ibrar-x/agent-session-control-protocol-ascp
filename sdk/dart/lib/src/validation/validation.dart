/// Marker library for the future validation helper surface.
///
/// Runtime-safe parse helpers are intentionally deferred to
/// `feature/dart-sdk-client`.
final class AscpValidationLibrary {
  const AscpValidationLibrary._();

  static const bool foundationOnly = true;
}
