/// Marker library for the future transport surface.
///
/// Live transport implementations are intentionally deferred to
/// `feature/dart-sdk-client`.
final class AscpTransportLibrary {
  const AscpTransportLibrary._();

  static const bool foundationOnly = true;
}
