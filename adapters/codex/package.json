{
  "name": "@ascp/adapter-codex",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "live": "npm run build --silent && node ./scripts/live-smoke.mjs",
    "test": "vitest run",
    "check": "npm run build && npm run test"
  },
  "dependencies": {
    "ascp-sdk-typescript": "file:../../sdks/typescript"
  },
  "devDependencies": {
    "@types/node": "^24.9.1",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
