# @ascp/adapter-codex

In-progress TypeScript workspace for the ASCP Codex runtime adapter. This package will map the official Codex runtime surface onto the frozen ASCP v0.1 contracts as implementation files are added in later tasks.

Current validation entrypoint:

```bash
bash tooling/scripts/validate_codex_adapter.sh
```
