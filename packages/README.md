# Packages

Shared implementation modules live here.

Dependency rule:

- packages may depend on `protocol/` assets only
- packages must not depend on `sdks/`, `adapters/`, `apps/`, or `services/`

Current placeholders:

- `core/`
- `transport/`
- `event-engine/`
- `auth/`
- `utils/`
