# Apps

User-facing clients and reference apps live here.

Dependency rule:

- apps may depend on `protocol/`, `packages/`, and `sdks/`
- apps must not reach into adapter internals directly

Current modules:

- `reference-client/`
- `web/` placeholder
